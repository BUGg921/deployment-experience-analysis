# 并行训练部署经验与慢卡场景归类 Skill（可复用模板）

> **用途**：从一批“慢卡分布场景 + 部署经验公式 + 打分/仿真验证结果”中，先抽取可复用的部署经验族，再分析每个经验族对应的典型慢卡场景模式。流程面向 128 GPU、256 GPU 及更大规模拓扑，不依赖固定卡数。

## 1. 核心术语中英文对照

| English | 中文 | 含义 |
|---|---|---|
| Experience Family | 经验族 / 部署经验族 | 由相似的并行部署经验形成的上层类别。 |
| Family Characteristics | 经验族部署特征（含主要部署经验） | 该经验族在并行策略空间中的共同倾向，并直接给出 2–3 条最主要、可执行的部署规则。 |
| Family-level Scenario Profile | 经验族场景总体特征 | 同一经验族下多个场景模式在慢卡分布空间中的总体共同特点。 |
| Scenario Motif | 场景模式 | 经验族内部进一步细化的典型慢卡分布形态。 |
| Slow-GPU Distribution Features | 慢卡分布特征 | 描述慢卡密度、覆盖范围、局部集中程度和均衡性的归一化特征。 |
| Affinity Group | 亲和组 | 由若干节点组成、内部通信关系更紧密的资源组。 |
| Intra-Affinity Imbalance | 亲和组内不均衡 | 同一亲和组内部不同节点慢卡比例的差异。 |
| Inter-Affinity Imbalance | 亲和组间不均衡 | 不同亲和组之间慢卡比例的差异。 |
| Scenario-specific Deployment Experience | 场景级部署经验 | 在经验族骨架之上，某一场景模式特有的比例、范围或约束。 |
| Deployment Rationale | 部署经验解释 | 结合场景分布与部署规则，对“为什么该策略在该场景下合理”给出的解释性归纳。 |
| Representative Scenarios | 代表场景 | 用于解释、复核该场景模式的典型样本。 |

> 除 Step 标题和最终表头外，正文统一使用中文术语。

## 2. 总体原则

整个分析始终分成两个空间：

1. **部署经验空间**：根据部署经验公式形成经验族；
2. **慢卡场景空间**：根据慢卡分布特征解释经验族内部的场景模式。

因果方向必须保持为：

$$
\text{部署经验公式}
\rightarrow
\text{经验族}
\rightarrow
\text{场景模式解释}
$$

不能用“覆盖几个亲和组”“慢卡总数是多少”等场景特征直接预先定义经验族。

另外：

- 打分最优和仿真最优只用于异常检查与后验验证，不用于反向修改经验公式；
- 优先保留比例、范围、`min/max` 和变量关系，避免把经验写死为仅适用于某一规模的绝对数值；
- 场景模式名称和阈值由当前数据归纳，不预先写死；
- “部署经验解释”属于帮助读者理解的性能机制解释，不等同于评分公式的严格证明。报告应在主表前统一说明这一点，不需要在每个单元格前重复添加“解释性归纳：”或“解释性假设：”等前缀。解释必须同时结合场景分布与并行策略机制，不能只给抽象结论。

---

## 3. Phase I — Data Preparation / 数据准备

### Step 1 — Input Normalization / 输入标准化

**目标**：把不同规模、不同来源的报告统一成同一数据结构。

需要标准化的字段包括：

- 总 GPU 数：$N$；
- 节点数：$M$；
- 每节点 GPU 数：$K$；
- 亲和组数量：$A$；
- 每个亲和组的节点数：$R$；
- 节点 $i$ 的慢卡数量：$c_i$；
- 亲和组 $j$ 的慢卡数量：$s_j$；
- 总慢卡数量：$S$；
- 部署经验公式：拆成 `PP-rule`、`TP-rule`、`DP-rule`、`EP-rule`、`MB-rule`、`active-rule` 等字段；
- 打分最优策略与仿真最优策略：仅作为验证字段保留。

同时检查基础守恒关系，例如：

$$
S=\sum_{i=1}^{M} c_i
$$

以及：

$$
S=\sum_{j=1}^{A} s_j.
$$

### Step 2 — Anomaly Screening / 异常筛选与隔离

**目标**：先把不可信或需要复核的样本隔离，再做经验归纳。

异常至少分成四类：

| 异常类型 | 检查内容 | 后续处理 |
|---|---|---|
| 数据异常 | 节点总慢卡数与报告总慢卡数不一致；亲和组汇总与节点汇总不一致；字段越界或缺失。 | 排除，修正后再进入分析。 |
| 经验公式异常 | 经验中的硬约束互相冲突；或经验公式明确排除了该场景自己的打分最优策略。 | 列入异常表，不参与经验族与场景模式归纳。 |
| 打分-仿真不一致 | 打分最优与仿真最优明显不同。 | 说明评分策略需复核，暂不用于可信经验归纳。 |
| 疑似经验离群 | 邻近场景经验高度一致，但单一场景出现无法解释的独特经验。 | 标记为 Review，不自动删除。 |

输出必须先给出 **异常汇总**：场景编号、异常类型、异常原因、是否排除。

---

## 4. Phase II — Experience Family Construction / 经验族构建

### Step 3 — Experience Feature Selection / 部署经验特征筛选

**不要预先规定一定使用 PP、TP、MB 划经验族。**

对 `PP / TP / DP / EP / MB / active GPU / 其他并行维度` 逐一评估，选择当前数据里最适合作为经验族主指标的特征。

建议依据四个标准：

| 标准 | 判断方式 |
|---|---|
| 区分度 | 不同场景之间，该经验规则是否发生明显变化。全部相同的维度不适合作为主指标。 |
| 稳定性 | 候选经验族内部，该规则是否保持稳定的方向、比例或范围。 |
| 独立性 | 如果某维度主要由其他维度派生，例如 $DP=N/(PP\times TP)$，优先作为辅助规则。 |
| 可迁移性 | 优先选择 `min/max`、比例、范围和变量关系，而不是仅在当前规模成立的绝对常数。 |

采用**最小解释集合**：从区分度最高的部署经验特征开始，逐步加入能显著减少混合规则的特征，直到主要经验模式稳定分开；其余特征作为辅助规则。

### Step 4 — Experience Family Construction / 部署经验族划分

**目标**：只根据部署经验的主要特征形成经验族。

具体做法：

- 先把表达方式规范化，例如 `PP=1`、`PP=min`、`PP 偏小` 可归入“浅 PP”方向，但原始公式仍需保留；
- 如果主要经验方向明显相反，例如 `PP=1` 与 `PP>=4`、`MB=1` 与 `MB>=2PP`，应划为不同经验族；
- DP、EP、active GPU 等辅助规则用于检查经验是否相容，以及经验族内部细化；
- 此步骤**不使用**慢卡密度、亲和组覆盖率、局部集中程度等场景特征来定义经验族。

### Step 5 — Family-level Experience Abstraction / 经验族级部署经验抽象

经验族级部署经验不是所有成员公式的机械交集，而是成员经验中稳定的共同方向。

例如，同一经验族中可能出现：

$$
PP\ge4,\qquad TP\le2,\qquad MB\ge2PP
$$

$$
PP\ge4,\qquad TP\le\frac{PP}{2},\qquad MB\ge2PP
$$

以及：

$$
PP\ge2,\qquad TP\le\frac{PP}{2},\qquad MB\ge\max(PP,8).
$$

经验族级别可以抽象为：

- PP 倾向中等或较深；
- TP 相对 PP 较小；
- MB 相对 PP 较大，并常随 PP 增长。

在最终主表中，**不再单独设置“经验族级部署经验”一列**。应把这一步得到的主要部署经验直接并入“经验族部署特征”：

1. 先给出简洁的结构类型画像，例如“深流水线型”“浅流水线型”；
2. 再列出 2–3 条最主要、可执行的 PP/TP/MB（或当前数据中最突出参数）的取值方向或关系；
3. DP、EP、active GPU 等共同硬约束、派生关系或辅助规则保留在正文，不重复放入主表。

具体上下界仍保留到场景级部署经验中。

---

## 5. Phase III — Scenario Motif Discovery / 场景模式发现

### Step 6 — Scenario Feature Construction / 场景特征构造

场景特征尽量使用**无量纲比例**，保证 128 GPU、256 GPU 及更大规模可以复用。

#### 6.1 全局慢卡率

定义：

- $S$：当前场景总慢卡数量；
- $N$：集群总 GPU 数量。

$$
\rho=\frac{S}{N}
$$

物理含义：整个集群中慢卡所占比例，表示慢卡问题的总体严重程度。

#### 6.2 节点覆盖率

定义：

- $c_i$：节点 $i$ 上的慢卡数量；
- $M$：节点总数；
- $\#\{i:c_i>0\}$：至少包含 1 张慢卡的节点数量。

$$
C_N=\frac{\#\{i:c_i>0\}}{M}
$$

物理含义：慢卡扩散到了多少比例的节点。

#### 6.3 亲和组覆盖率

定义：

- $s_j$：亲和组 $j$ 内的慢卡数量；
- $A$：亲和组总数；
- $\#\{j:s_j>0\}$：至少包含 1 张慢卡的亲和组数量。

$$
C_A=\frac{\#\{j:s_j>0\}}{A}
$$

物理含义：慢卡扩散到了多少比例的亲和组。

#### 6.4 单节点最大慢卡比例

定义：

- $c_i$：节点 $i$ 上的慢卡数量；
- $K$：每个节点的 GPU 数量。

$$
R_{\max}=\max_i\frac{c_i}{K}
$$

物理含义：所有节点中，**慢卡占比最高的那个节点，其慢卡比例是多少**。它反映慢卡是否高度集中在某个单独节点。例如 $R_{\max}=1$ 表示至少存在一个节点的全部 GPU 都是慢卡。

#### 6.5 亲和组内曼哈顿不均衡

先定义亲和组 $j$ 内第 $p$ 个节点的慢卡比例：

- $c_{j,p}$：亲和组 $j$ 内第 $p$ 个节点的慢卡数量；
- $K$：每个节点的 GPU 数；
- $R$：每个亲和组包含的节点数。

$$
r_{j,p}=\frac{c_{j,p}}{K}
$$

对亲和组 $j$，定义组内两两慢卡比例差异的平均值：

$$
D_{\mathrm{intra},j}
=\frac{2}{R(R-1)}
\sum_{p<q}|r_{j,p}-r_{j,q}|.
$$

整个场景取最严重亲和组：

$$
D_{\mathrm{intra}}=\max_jD_{\mathrm{intra},j}.
$$

当每个亲和组恰好包含两个节点时：

$$
D_{\mathrm{intra},j}=|r_{j,1}-r_{j,2}|.
$$

物理含义：同一个亲和组内部，不同节点承担的慢卡负担是否均衡。值越大，说明某个亲和组内部更容易出现“一个节点很慢、另一个节点相对正常”的局部失衡。

#### 6.6 亲和组间曼哈顿不均衡

先定义亲和组慢卡比例：

- $s_j$：亲和组 $j$ 内慢卡数量；
- $R\times K$：一个亲和组包含的 GPU 总数；
- $A$：亲和组总数。

$$
a_j=\frac{s_j}{RK}.
$$

再定义亲和组之间两两慢卡比例差异的平均值：

$$
D_{\mathrm{aff}}
=\frac{2}{A(A-1)}
\sum_{p<q}|a_p-a_q|.
$$

物理含义：不同亲和组之间慢卡负担是否均衡。值越大，说明慢卡更集中在少数亲和组；值越小，说明慢卡在不同亲和组之间更均匀。

> 熵可以作为辅助特征保留，但如果熵定义无法区分 0 张与 1 张慢卡，则不能单独承担场景划分。

### Step 7 — Scenario Feature Selection / 场景特征筛选

这一过程与 Step 3 对称：**不预设一定由 $C_A$ 或 $D_{\mathrm{intra}}$ 划场景模式。**

在每个经验族内部，比较：

- $\rho$：全局慢卡率；
- $C_N$：节点覆盖率；
- $C_A$：亲和组覆盖率；
- $R_{\max}$：单节点最大慢卡比例；
- $D_{\mathrm{intra}}$：亲和组内不均衡；
- $D_{\mathrm{aff}}$：亲和组间不均衡。

优先寻找**边界对照场景**：总慢卡率和覆盖范围接近，但经验族不同的样本最有信息量。

完成比较后，应进一步把场景特征分成两层：

- **主要区别特征**：真正决定该场景模式与邻近模式差异的结构性特征，应优先放在最终主表中；
- **辅助区别特征**：用于描述严重程度、边界或样本范围，但不是该模式的核心定义。

例如，亲和组覆盖率、亲和组内不均衡等结构性特征往往更可能成为主要区别特征；全局慢卡率、单节点最大慢卡比例等连续量如果只是在样本间波动，则更适合作为辅助特征。**但主/辅身份不能预先写死**：如果某个连续量确实决定了场景边界，它也应提升为主要区别特征。

可以使用浅层 decision tree 或 rule list 辅助发现候选特征，但这些规则只用于解释，不反向定义经验族。

### Step 8 — Scenario Motif Extraction / 场景模式提取与场景级经验

在每个经验族内部，根据 Step 7 选出的突出场景特征形成 A1、A2、B1、B2 等场景模式。

每个场景模式必须给出：

- 场景模式名称；
- 场景数量；
- 慢卡分布特征，其中先列**主要区别特征**，再列必要的**辅助区别特征**；
- 代表场景；
- 场景级部署经验；
- 部署经验解释。

场景级经验写成：

$$
E_{\mathrm{scenario}}
=
E_{\mathrm{family}}
+
\Delta E_{\mathrm{scenario}}.
$$

含义是：先继承经验族共同骨架，再增加该场景模式特有的比例、范围或上下界。

若某个场景模式只有单一样本，应标记为“边界/特殊场景模式”，避免过度泛化。

部署经验解释应逐条对应当前场景模式与部署规则，并采用**面向不了解并行训练读者的性能解释**。推荐按“场景特点 → 主要性能问题 → 为什么调整某个并行参数 → 这样调整带来什么性能收益/代价”的顺序说明，而不是只写“局部隔离收益下降”“同步代价较大”等抽象判断。

解释时至少考虑以下机制，并只使用与当前场景和经验规则相关的部分：

- **PP（流水并行）**：增大 PP 会把模型切成更多流水阶段，可降低单阶段模型/显存压力，并可能把局部慢区的影响限制在较少阶段；但阶段越多，流水线气泡、跨阶段传输和阶段间等待通常越明显。减小 PP 则减少这些流水依赖，适合慢卡已经广泛分布、难以通过更多流水阶段实现局部隔离的场景。
- **TP（张量并行）**：增大 TP 会把同一层计算分摊到更多 GPU，降低单卡计算与显存压力；但同一 TP 组内需要更频繁同步，一张慢卡可能使整个同步组等待。因此局部慢卡明显时常倾向较小 TP；当 PP 很浅、需要更多模型内并行来分担计算时，可以采用中等或较大的 TP，但应避免同步组过大。
- **DP（数据并行）**：增大 DP 会增加并行副本，可提高整体吞吐并承接 PP/TP 减小后释放的 GPU；代价是副本之间需要梯度同步，因此应结合通信代价和其是否主要由 `N/(PP×TP)` 等约束派生来解释。
- **MB（微批次数）**：较大的 MB 可以更充分地填充深流水线、降低流水线气泡比例；当 PP 很浅或 PP=1 时，不需要依靠大量微批次填流水，因此 MB 可以较小。较大的 MB 也可能增加激活/显存压力，应结合原经验约束说明。
- **EP（专家并行，如存在）**：说明其是否用于分散专家计算/容量，以及扩大 EP 后的专家通信和负载均衡代价；若当前经验中 EP 只是共同范围或硬约束，则不要强行把它解释成 Family 的主要机制。

如果使用“局部隔离收益下降”这样的概括，必须紧接着解释其具体含义，例如：“慢卡已经分散到多个亲和组/大量节点后，即使继续增加 PP，也很难把慢卡限制在少数流水阶段，因此新增阶段带来的气泡和等待可能超过隔离收益。”

表格单元格中**不要添加**“解释性归纳：”等固定前缀；应直接进入性能解释。严格性边界在主表前统一说明即可。

### Step 9 — Family-level Scenario Abstraction / 经验族场景总体特征抽象

**必须在场景模式已经划分完成之后再做。**

对同一经验族下的全部场景模式进行二次归纳，提炼其慢卡场景空间中的主要共同特点。这里不是求严格交集，而是总结主要特征。

经验族场景总体特征只保留**能够概括该经验族的主要场景特征**，不要把所有可计算指标都堆进去。辅助统计应留在场景模式的“慢卡分布特征”中。

推荐使用“**抽象概括 + 主特征数据**”的写法，例如：

> **广覆盖、整体较均衡为主**（主要表现为大量场景覆盖全部亲和组；当只覆盖部分亲和组时，亲和组内两节点慢卡比例差通常较小）。

或者：

> **局部化、亲和组内不均衡更突出**（主要表现为慢卡只覆盖少数亲和组；双亲和组场景中，亲和组内两节点慢卡比例差 `D_intra` 往往较大）。

如果在总体特征中使用变量名，必须先写中文物理含义，再给变量，例如：

- `覆盖 2/4 个亲和组：C_A = 0.50`；
- `亲和组内两节点慢卡比例差较大：D_intra ≥ 0.50`。

括号或后半句中的数据只用于支撑**主要特征**，不再罗列全局慢卡率、节点覆盖率、最大节点慢卡比例等所有辅助统计。

语言上可以使用“通常、主要、以……为主、当……时往往……”。只有所有场景模式都满足时，才使用“必须、均满足”。

---

## 6. 最终输出主表规范

在主表之前，应先增加一个简短的 **“并行参数对性能的直观影响”** 说明。其作用不是重新推导经验，而是帮助没有并行训练背景的读者理解后续“部署经验解释”列。说明应尽量控制在 PP、TP、DP、MB 四个核心参数，只有当 EP 在当前数据中确实具有区分作用时再补充 EP。

标准 Markdown 表格不支持合并单元格和精确列宽，因此最终报告的主表应使用 **HTML table 嵌入 Markdown**。

要求：

- 同一经验族跨多个场景模式时，`经验族`、`经验族部署特征`、`经验族场景总体特征` 使用 `rowspan` 合并；
- “场景数量”单独成列，放在“场景模式”右侧；
- 慢卡分布特征、场景级部署经验、部署经验解释使用较宽列；
- 经验族、场景数量使用较窄列；
- 单元格中每条规则都使用 `；<br>` 换行，便于逐条比较；
- **HTML 表格内部不要使用 LaTeX 定界符**（如 `$...$`、`$$...$$`），因为部分 Markdown 渲染器不会在 HTML table 内继续解析 LaTeX。表格中统一使用可直接显示的数学符号文本，例如 `PP ≥ 4`、`TP/DP ≤ 1/4`、`DP = N/(PP × TP)`、`ρ = 0`；下标可使用 HTML，如 `C<sub>A</sub> = 0.50`、`D<sub>intra</sub> ≥ 0.50`。正文中的独立公式仍统一使用 `$$...$$`；
- 慢卡分布特征必须先写中文物理含义，再写变量名与数值，例如：`覆盖 2/4 个亲和组：C<sub>A</sub> = 0.50；`；并在同一单元格中先列“主要区别特征”，再列必要的“辅助区别特征”；
- `R_{\max}` 的中文统一写成“单节点最大慢卡比例”，不要只写“热点”；
- 主表之前增加一段简短的“并行参数对性能的直观影响”，用通俗语言解释 PP、TP、DP、MB（以及必要时 EP）的基本作用，为最右侧解释提供阅读基础；
- 最右侧增加“部署经验解释”列，对每个场景模式逐条解释其主要部署策略为什么与当前慢卡分布相匹配；
- 部署经验解释直接写性能机制，不加“解释性归纳：”等前缀；报告在表格前统一声明该列用于帮助理解，不等同于评分公式的严格证明；
- 解释应优先回答“为什么 PP 要更深/更浅”“为什么 TP 要更小/中等/更大”“为什么 MB 要更大/更小”“DP 为什么扩大或主要由约束派生”，并明确相应的性能收益与代价。

推荐列顺序：

1. Experience Family / 经验族；
2. Family Characteristics / 经验族部署特征（含主要部署经验）；
3. Family-level Scenario Profile / 经验族场景总体特征；
4. Scenario Motif / 场景模式；
5. Scenario Count / 场景数量；
6. Slow-GPU Distribution Features / 慢卡分布特征；
7. Scenario-specific Deployment Experience / 场景级部署经验；
8. Representative Scenarios / 代表场景；
9. Deployment Rationale / 部署经验解释。

---

## 7. 最终主表每一列分别回答什么问题？

| 列 | 主要职责 / 回答的问题 |
|---|---|
| Experience Family / 经验族 | 哪些部署经验可以归为同一个上层类别？这是主分类标签，由部署经验空间产生。 |
| Family Characteristics / 经验族部署特征（含主要部署经验） | 这是一种什么类型的并行结构，以及部署时首先抓住哪些主要规则？先给出“深流水线型/浅流水线型”等定性画像，再列 2–3 条最主要、可执行的参数取值方向或比例关系。 |
| Family-level Scenario Profile / 经验族场景总体特征 | 采用这种部署经验的场景，在慢卡分布上总体呈现什么**主要特点**？先给抽象概括，再只用主特征对应的数据支撑；若出现变量名，必须先写中文物理含义。 |
| Scenario Motif / 场景模式 | 该经验族内部还能分成哪些典型慢卡分布形态？只写模式名称，不再把数量写进名称。 |
| Scenario Count / 场景数量 | 每一个场景模式包含多少个有效场景？用于判断该模式的样本支撑强弱。 |
| Slow-GPU Distribution Features / 慢卡分布特征 | 这个场景模式由哪些突出特征识别？先列主要区别特征，再列必要的辅助区别特征；必须“中文物理含义在前，变量和数值在后”。 |
| Scenario-specific Deployment Experience / 场景级部署经验 | 该场景模式在经验族主要规则之上还需要哪些具体比例、上下界或补充规则？ |
| Representative Scenarios / 代表场景 | 哪些原始场景最适合用于解释和复核这一场景模式？保留可追溯性。 |
| Deployment Rationale / 部署经验解释 | 为什么该场景模式对应这样的部署经验？面向不了解并行训练的读者，从性能机制解释 PP/TP/DP/MB（必要时 EP）的取值：该参数改变了什么计算、同步、流水线或吞吐开销，为什么与当前慢卡分布相匹配。不要只写抽象标签，也不要在单元格前重复“解释性归纳：”；严格性边界在主表前统一说明。 |

这三列尤其容易混淆，其职责应明确区分：

- **经验族部署特征（含主要部署经验）**回答“这是一种什么类型的并行结构，以及部署时首先抓住哪些主要规则”；
- **经验族场景总体特征**回答“这一类部署经验总体对应什么样的慢卡分布”；
- **场景模式**回答“这些总体分布还能进一步分成哪些典型形态”。

---

## 8. 跨规模复用原则

应用到 256 GPU 时，不复制 128 GPU 报告里的经验族数量、阈值或场景模式名称，只复用流程：

1. 重新做异常筛选；
2. 重新选择最突出的部署经验特征；
3. 重新形成经验族；
4. 按新的 $N,M,K,A,R$ 重新计算场景特征；
5. 重新选择最突出的场景特征；
6. 重新形成场景模式；
7. 重新抽象经验族场景总体特征；
8. 重新生成带合并单元格、场景数量与部署经验解释的 9 列主表。
