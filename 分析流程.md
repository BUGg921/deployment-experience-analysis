本实验主要包含以下两部分内容：

1. **基于参考案例的 256 GPU 场景分析报告生成**
   利用 ChatGPT 网页版，以现有的 Skills 文档及 128 GPU 场景分析报告为参照范例，对 256 GPU 场景进行分析。模型将依据 Skills 规范，生成结构类似于 128 GPU 报告的分析内容，并最终产出“Family–Scenario–Experience”对照表。
   *   **输入数据**：
       (1) 256 GPU 部署经验汇总；
       (2) Skills 文档；
       (3) 128 GPU 分析报告（作为参考范例）。

2. **256 GPU 与 128 GPU 场景的差异分析及策略细化**
   利用 ChatGPT 网页版，深入分析 256 GPU 场景相较于 128 GPU 场景的主要经验差异。
   以 128 卡 GPU 的经验表格为基准，融合 256 卡的新增实践进行迭代：对通用部分做合并收敛，针对新特性做细化拆解（如重新界定快慢卡场景、优化部署策略），从而形成更完整的部署经验体系。
   *   **输入数据**：
       (1) 128 GPU 与 256 GPU 的部署经验汇总文件；
       (2) 两个场景各自的分析报告。