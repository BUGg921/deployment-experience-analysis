# 128 GPU / 100 场景：部署经验与慢卡场景关系分析报告

> **分析依据**：`100场景_慢卡分布_熵_最优策略_部署经验汇总.md`。本报告按最新版 Skill 执行：先异常筛选，再从部署经验中形成经验族；随后分析慢卡分布特征、提取场景模式；最后总结经验族场景总体特征，并给出场景—经验之间的解释性对应关系。

## 1. 核心术语中英文对照

| English | 中文 |
|---|---|
| Experience Family | 经验族 / 部署经验族 |
| Family Characteristics | 经验族部署特征（含主要部署经验） |
| Family-level Scenario Profile | 经验族场景总体特征 |
| Scenario Motif | 场景模式 |
| Scenario Count | 场景数量 |
| Slow-GPU Distribution Features | 慢卡分布特征 |
| Affinity Group | 亲和组 |
| Intra-Affinity Imbalance | 亲和组内不均衡 |
| Inter-Affinity Imbalance | 亲和组间不均衡 |
| Scenario-specific Deployment Experience | 场景级部署经验 |
| Deployment Rationale | 部署经验解释 |
| Representative Scenarios | 代表场景 |

> 除 Step 名称和最终表头外，正文以下统一使用中文术语。

---

## 2. Step 1 — Input Normalization / 输入标准化

本批实验拓扑为：

- 总 GPU 数：$N=128$；
- 节点数：$M=8$；
- 每节点 GPU 数：$K=16$；
- 亲和组数量：$A=4$；
- 每个亲和组包含 $R=2$ 个节点，即 32 GPU；
- 全局 batch：$B=128$。

共同 GPU 使用约束为：

$$
PP\times TP\times DP=128.
$$

另外，EP、PP、DP、MB 还受到整除和显存约束。由于 `active_gpu=N` 与下面的关系在大量经验中属于共同约束：

$$
DP=\frac{N}{PP\times TP},
$$

因此它们不适合作为第一层经验族主指标。

---

## 3. Step 2 — Anomaly Screening / 异常筛选与隔离

### 3.1 异常汇总

| 场景 | 异常类型 | 原因 | 是否参与后续归纳 |
|---|---|---|---|
| s005 | 经验公式异常 | 经验包含 $PP\cdot TP^2\le N/20$。代入该场景打分/仿真最优 $PP=4,TP=2,N=128$，得到 $16\le6.4$，不成立。 | 否。保留在异常表中，等待经验公式复核。 |

### 3.2 打分与仿真一致性

本批 100 个场景中，打分最优与仿真最优一致，可作为评分策略已通过当前候选集验证的参考。因此后续只隔离 s005 这一条已确认经验公式异常，使用 **99 个有效场景**进行经验归纳。

有效场景数量：

- 经验族 A：15 个；
- 经验族 B：84 个。

---

## 4. Step 3 — Experience Feature Selection / 部署经验特征筛选

这一批数据没有预先规定必须使用 PP、TP、MB。对经验公式中的主要并行维度进行比较后，发现：

### 4.1 主指标

**PP、TP、MB** 对两类经验的区分最明显：

- 一类经验倾向中等/较深 PP、较小 TP、较大的 MB；
- 另一类经验倾向浅 PP、适中的 TP、较小或最小 MB。

### 4.2 辅助指标

**DP、EP、active GPU** 更适合作为辅助规则：

- DP 大量由下面关系派生：

$$
DP=\frac{N}{PP\times TP};
$$

- `active_gpu=N` 基本属于共同硬约束；
- EP 多数围绕 $E/C$ 或其附近范围变化，更适合做经验族内部细化。

因此，本批 128 GPU 数据的最小解释集合为：

$$
\{PP,TP,MB\}\quad\text{作为主指标},
$$

$$
\{DP,EP,active\}\quad\text{作为辅助规则}.
$$

> 该选择只针对当前 128 GPU 数据成立。应用到 256 GPU 时必须重新执行 Step 3，而不是直接沿用这一指标集合。

---

## 5. Step 4 — Experience Family Construction / 部署经验族划分

基于部署经验公式的主要倾向，99 个有效场景可以先压缩为两个上层经验族。

| 经验族 | 有效场景数 | 经验族部署特征 | 参考最优策略（仅验证） |
|---|---:|---|---|
| A | 15 | **深流水线型**；<br>定性：PP 较深；TP 较小；MB 较大；<br>主要规则：PP 取中等/较深；TP 相对 PP 保持较小；MB 随 PP 增大以填充流水线。 | `PP4_TP2_DP16_EP8_MB8` |
| B | 84 | **浅流水线型**；<br>定性：PP 较浅；TP 中等；MB 较小；<br>主要规则：PP 常为 1/最小可行值；TP 常约为 4 或由 TP/DP 比例约束；MB 常为 1/最小可行值。 | `PP1_TP4_DP32_EP8_MB1` |

这里的经验族是**由部署经验形成的**，而不是根据慢卡覆盖多少个亲和组形成的。

---

## 6. Step 5 — Family-level Experience Abstraction / 经验族级部署经验抽象

这一层只提取**真正能够代表整个经验族的主要部署经验**。在最终主表中，这些主要规则不再单独设置“经验族级部署经验”一列，而是直接并入“经验族部署特征”：先给出“深流水线型/浅流水线型”等定性画像，再列出 2–3 条最主要的 PP、TP、MB 规则。共同硬约束、派生关系或只在部分场景中变化的规则继续作为辅助信息保留在正文，不进入主表。

### 6.1 经验族 A

**主要部署经验**：

- PP：倾向中等或较深，而不是最浅流水线；
- TP：相对 PP 保持较小，避免把过多 GPU 绑定在同一个高频同步组中；
- MB：随 PP 增大而增大，用于填充较深流水线。

**辅助规则**：

- DP 通常由下面的满卡关系补足：

$$
DP=\frac{N}{PP\times TP};
$$

- EP 多在 $E/C$ 到约 $2E/C$ 的范围内。

### 6.2 经验族 B

**主要部署经验**：

- PP：倾向较浅，常为 $PP=1$ 或最小可行值；
- TP：保持中等规模，常围绕 4 或由比例约束确定；
- MB：倾向较小，常为 $MB=1$ 或最小可行值。

**辅助规则**：

- DP 主要由下面的满卡关系派生，因此整体较大：

$$
DP=\frac{N}{PP\times TP};
$$

- EP 主要围绕 $E/C$ 及其附近范围。

> 最终主表中，“经验族部署特征”同时承担两层信息：第一层是“深流水线型/浅流水线型”等结构画像；第二层紧跟 2–3 条主要部署规则。辅助规则不重复写入主表。

---

## 7. Step 6 — Scenario Feature Construction / 场景特征构造

对每个场景构造以下归一化特征。

### 7.1 全局慢卡率

其中：

- $S$：当前场景总慢卡数量；
- $N$：集群总 GPU 数量。

$$
\rho=\frac{S}{N}
$$

物理含义：整个集群中慢卡所占比例，用于描述慢卡问题的总体严重程度。

### 7.2 节点覆盖率

其中：

- $c_i$：节点 $i$ 上的慢卡数量；
- $M$：节点总数；
- $\#\{i:c_i>0\}$：至少包含 1 张慢卡的节点数量。

$$
C_N=\frac{\#\{i:c_i>0\}}{M}
$$

物理含义：慢卡扩散到了多少比例的节点。

### 7.3 亲和组覆盖率

其中：

- $s_j$：亲和组 $j$ 内的慢卡数量；
- $A$：亲和组总数；
- $\#\{j:s_j>0\}$：至少包含 1 张慢卡的亲和组数量。

$$
C_A=\frac{\#\{j:s_j>0\}}{A}
$$

物理含义：慢卡扩散到了多少比例的亲和组。

### 7.4 单节点最大慢卡比例

其中：

- $c_i$：节点 $i$ 上的慢卡数量；
- $K$：每个节点的 GPU 数量。

$$
R_{\max}=\max_i\frac{c_i}{K}
$$

物理含义：所有节点中，**慢卡占比最高的那个节点，其慢卡比例是多少**。例如 $R_{\max}=1$ 表示至少存在一个节点的 16 张 GPU 全部为慢卡；$R_{\max}=0.5$ 表示最严重节点中有一半 GPU 为慢卡。

### 7.5 亲和组内曼哈顿不均衡

先定义亲和组 $j$ 内第 $p$ 个节点的慢卡比例：

- $c_{j,p}$：亲和组 $j$ 内第 $p$ 个节点的慢卡数量；
- $K=16$：每节点 GPU 数；
- $R=2$：当前拓扑中每个亲和组包含的节点数。

$$
r_{j,p}=\frac{c_{j,p}}{K}
$$

一般情况下：

$$
D_{\mathrm{intra},j}
=\frac{2}{R(R-1)}
\sum_{p<q}|r_{j,p}-r_{j,q}|.
$$

当前每个亲和组恰好包含两个节点，因此可以简化为：

$$
D_{\mathrm{intra},j}
=
|r_{j,1}-r_{j,2}|.
$$

整个场景取最严重亲和组：

$$
D_{\mathrm{intra}}
=
\max_jD_{\mathrm{intra},j}.
$$

物理含义：同一个亲和组内部，两个节点的慢卡比例最大能相差多少。例如 $D_{\mathrm{intra}}=0.50$ 表示至少有一个亲和组中，两节点的慢卡率相差 50 个百分点。

### 7.6 亲和组间曼哈顿不均衡

先定义亲和组慢卡比例：

- $s_j$：亲和组 $j$ 内慢卡数量；
- $R\times K=32$：一个亲和组包含的 GPU 总数；
- $A=4$：亲和组总数。

$$
a_j=\frac{s_j}{RK}.
$$

再定义亲和组之间两两慢卡比例差异的平均值：

$$
D_{\mathrm{aff}}
=\frac{2}{A(A-1)}
\sum_{p<q}|a_p-a_q|.
$$

物理含义：不同亲和组之间的慢卡负担是否均衡。值越大，说明慢卡越集中在少数亲和组；值越小，说明不同亲和组之间更均匀。

> **注意**：Step 6 的作用是把候选场景特征统一计算出来，并不意味着这些特征都会进入最终场景模式定义。最终归纳时只保留真正具有区分作用的“主要区别特征”；其余指标仅作为“辅助区别特征”，用于描述严重程度、边界或代表场景。

---

## 8. Step 7 — Scenario Feature Selection / 场景特征筛选

本批数据虽然为每个场景计算了多个归一化特征，但**这些特征不应等权使用**。最终场景模式应优先保留能够把相邻模式真正区分开的结构性特征，并把容易随场景连续波动的指标降为辅助特征。

### 8.1 当前数据中的特征优先级

当前 128 GPU 数据中，最稳定的结构性区分主要来自：

1. $C_A$：亲和组覆盖率——慢卡究竟集中在少数亲和组，还是已经扩散到全部亲和组；
2. $D_{\mathrm{intra}}$：亲和组内不均衡——同一亲和组内部两个节点的慢卡负担是否明显失衡。

以下指标更适合作为辅助描述：

- $C_N$：节点覆盖率，用来补充说明慢卡扩散到多少节点；
- $\rho$：全局慢卡率，用来说明慢卡问题的总体严重程度；
- $R_{\max}$：单节点最高慢卡比例，用来说明是否存在“几乎全慢”或“全慢”的严重节点；
- $D_{\mathrm{aff}}$：亲和组间不均衡，用来补充说明不同亲和组之间负担是否接近。

这里的“主要/辅助”不是永久固定的。例如在 B3 中，$\rho$ 与 $R_{\max}$ 恰好能够把它与 A3、B2 区分开，因此会升级为该模式的主要区别特征；但在 B1 中，$\rho$ 从 3.13% 到 100% 都可能出现，因此它只能作为辅助描述，不能作为模式边界。

### 8.2 为什么不能只看亲和组覆盖率

`s032` 与 `s033` 是一组边界对照：

| 场景 | 经验族 | 全局慢卡率 $\rho$ | 亲和组覆盖率 $C_A$ | 亲和组内最大不均衡 $D_{\mathrm{intra}}$ |
|---|---|---:|---:|---:|
| s032 | A | 31.25% | 0.50（覆盖 2/4 个亲和组） | 0.50 |
| s033 | B | 32.03% | 0.50（覆盖 2/4 个亲和组） | 0.0625 |

两者慢卡率和亲和组覆盖率非常接近，但 s032 的受影响亲和组内部明显更不均衡，因此落入不同经验族。

`s042` 与 `s043` 再次出现相同现象：

| 场景 | 经验族 | 全局慢卡率 $\rho$ | 亲和组覆盖率 $C_A$ | 亲和组内最大不均衡 $D_{\mathrm{intra}}$ |
|---|---|---:|---:|---:|
| s042 | A | 41.41% | 0.50（覆盖 2/4 个亲和组） | 0.6875 |
| s043 | B | 42.19% | 0.50（覆盖 2/4 个亲和组） | 0 |

因此，对这类边界场景，$D_{\mathrm{intra}}$ 比 $\rho$ 更具有区分力；$\rho$ 更适合用来描述问题严重程度，而不是首先定义场景模式。

### 8.3 主要区别特征与辅助区别特征的使用原则

- **主要区别特征**：优先回答“为什么这个场景属于这个 Motif，而不是相邻 Motif”。通常只保留 1–3 个最有区分力的指标；
- **辅助区别特征**：用于描述当前样本的数值范围、严重程度或边界，不默认作为硬阈值；
- 如果一个辅助特征在新的 256 GPU 场景中发生一定波动，只要主要区别特征仍然成立，就不应轻易把场景划到另一个 Motif；
- 只有当某个辅助特征在当前数据中确实承担了边界作用时，才将它升级为该 Motif 的主要区别特征。

---

## 9. Step 8 — Scenario Motif Extraction / 场景模式提取

下面每个场景模式都先给出“主要区别特征”，再给出“辅助区别特征”。主要区别特征用于定义该 Motif；辅助特征仅描述当前 128 GPU 样本中的常见范围，不应直接固化为跨规模硬阈值。

### 9.1 经验族 A

#### A1 — 无慢卡基线

**主要区别特征**：

- 全局无慢卡：$\rho=0$。

**辅助区别特征**：

- 无受影响节点：$C_N=0$；
- 无受影响亲和组：$C_A=0$。

代表场景：s001。

#### A2 — 单亲和组局部化

**主要区别特征**：

- 仅覆盖 1/4 个亲和组：$C_A=0.25$；
- 慢卡明显局部化在单个亲和组内。

**辅助区别特征**：

- 仅影响 1–2/8 个节点：$C_N=0.125\sim0.25$；
- 当前样本全局慢卡率为 0.78%–21.88%：$\rho=0.78\%\sim21.88\%$；
- 当前样本单节点最高慢卡比例为 6.25%–100%：$R_{\max}=6.25\%\sim100\%$；
- 亲和组内不均衡范围较宽：$D_{\mathrm{intra}}=0\sim0.875$，因此它不是 A2 的必要条件。

代表场景：s003、s012、s013、s023。

#### A3 — 双亲和组局部化 / 不均衡主导

**主要区别特征**：

- 覆盖 2/4 个亲和组：$C_A=0.50$；
- 亲和组内不均衡明显：7 个场景中 6 个满足 $D_{\mathrm{intra}}\ge0.50$。

**辅助区别特征**：

- 影响 2–4/8 个节点：$C_N=0.25\sim0.50$；
- 当前样本全局慢卡率为 9.38%–41.41%：$\rho=9.38\%\sim41.41\%$；
- 当前样本单节点最高慢卡比例为 25%–100%：$R_{\max}=25\%\sim100\%$。

代表场景：s010、s015、s032、s042。

### 9.2 经验族 B

#### B1 — 全亲和组广泛覆盖

**主要区别特征**：

- 覆盖全部 4/4 个亲和组：$C_A=1$。

**辅助区别特征**：

- 至少一半节点受影响：$C_N=0.50\sim1.00$；
- 当前样本全局慢卡率跨度很大：$\rho=3.13\%\sim100\%$，因此慢卡率不是该模式的主要边界；
- $D_{\mathrm{intra}}=0\sim1$，说明组内均衡性也不是 B1 的必要条件。

代表场景：s021、s038、s081、s100。

#### B2 — 部分亲和组覆盖但组内较均衡

**主要区别特征**：

- 覆盖 2–3/4 个亲和组：$C_A=0.50\sim0.75$；
- 受影响亲和组内部较均衡：$D_{\mathrm{intra}}\le0.25$。

**辅助区别特征**：

- 影响 4–6/8 个节点：$C_N=0.50\sim0.75$；
- 当前样本全局慢卡率为 6.25%–72.66%：$\rho=6.25\%\sim72.66\%$，跨度较大，不作为主要边界。

代表场景：s007、s033、s043、s063。

#### B3 — 部分亲和组覆盖 + 中高负担 / 强局部集中

**主要区别特征**：

- 覆盖 2–3/4 个亲和组：$C_A=0.50\sim0.75$；
- 亲和组内不均衡较高：$D_{\mathrm{intra}}=0.375\sim1.00$；
- 慢卡总体负担已经较重：当前 5 个场景的 $\rho=34.38\%\sim61.72\%$，且每个场景都至少存在一个全慢节点，即 $R_{\max}=1$。

这里第三条承担的是与 A3、B2 的主要边界作用，因此在 B3 中，$\rho$ 与 $R_{\max}$ 不再只是辅助描述。

**辅助区别特征**：

- 影响 3–5/8 个节点：$C_N=0.375\sim0.625$。

代表场景：s035、s036、s046、s062。

---

## 10. Step 9 — Family-level Scenario Abstraction / 经验族场景总体特征抽象

### 10.1 经验族 A

**总体呈现局部化；当慢卡扩展到两个亲和组时，亲和组内不均衡通常更明显。**（除无慢卡基线外，14/14 个慢卡场景只覆盖 1–2/4 个亲和组；双亲和组场景中 6/7 的**亲和组内两节点慢卡比例差** $D_{\mathrm{intra}}\ge0.50$。）

因此，经验族 A 的主场景特征可以概括为：**覆盖范围较局部 + 双亲和组情况下常伴随明显组内失衡**。

### 10.2 经验族 B

**总体以广覆盖为主；若未覆盖全部亲和组，则通常表现为组内较均衡，或慢卡负担已经较高。**（84 个有效场景中 71 个覆盖全部 4/4 个亲和组；其余 13 个覆盖 2–3 个亲和组，其中 8 个的**亲和组内两节点慢卡比例差** $D_{\mathrm{intra}}\le0.25$；另外 5 个的**全局慢卡比例** $\rho=34.38\%\sim61.72\%$，且**单节点最高慢卡比例** $R_{\max}=1$。）

因此，经验族 B 的主场景特征可以概括为：**广覆盖；或在部分覆盖时呈现较强组内均衡性 / 较高整体慢卡负担**。这也解释了为什么 s033、s043 虽然只覆盖两个亲和组，仍然属于经验族 B。

---

## 11. 最终 Family–Scenario–Experience 对照表

### 11.1 先理解四类并行参数对性能的影响

为了让没有并行训练背景的读者也能理解最右侧的“部署经验解释”，这里先给出四个核心参数的性能直觉：

- **PP（Pipeline Parallelism，流水线并行）**：把模型按层切成多个前后相接的阶段。PP 越大，每个阶段承担的模型和计算越少，但阶段之间的串行依赖更多，也更容易产生流水线气泡和阶段间等待。
- **TP（Tensor Parallelism，张量并行）**：把同一层的矩阵计算拆到多张 GPU 上。TP 增大可以降低单卡计算和显存压力，但同一层的 GPU 需要频繁同步；如果其中有慢卡，其他 GPU 往往也要等待，因此 TP 过大容易放大慢卡的同步拖尾。
- **DP（Data Parallelism，数据并行）**：复制多份模型，同时处理不同数据。DP 增大主要用于提高吞吐，不会增加流水线阶段深度，但需要进行梯度同步。
- **MB（Micro-batch Number，微批次数）**：把一个 batch 拆成更多微批次。对于较深 PP，更多 MB 可以让不同流水阶段同时工作，减少流水线空转；当 PP 很浅或 PP=1 时，这种收益明显减弱。

> **说明 1**：同一经验族共享的单元格使用 `rowspan` 合并；“经验族部署特征”已合并主要部署经验，“场景数量”单独成列。  
> **说明 2**：表格位于 HTML 区域，因此其中的数学关系全部使用可直接显示的数学符号文本，不使用 LaTeX 源码。  
> **说明 3**：每条规则使用“；<br>”分行。最右侧解释从并行机制和性能开销出发说明为什么该场景更适合对应策略，它是帮助理解的性能解释，不等同于评分公式的严格证明。

<table>
  <colgroup>
    <col style="width:4%">
    <col style="width:16%">
    <col style="width:14%">
    <col style="width:9%">
    <col style="width:4%">
    <col style="width:17%">
    <col style="width:15%">
    <col style="width:6%">
    <col style="width:15%">
  </colgroup>
  <thead>
    <tr>
      <th>Experience Family / 经验族</th>
      <th>Family Characteristics / 经验族部署特征（含主要部署经验）</th>
      <th>Family-level Scenario Profile / 经验族场景总体特征</th>
      <th>Scenario Motif / 场景模式</th>
      <th>Scenario Count / 场景数量</th>
      <th>Slow-GPU Distribution Features / 慢卡分布特征（主要/辅助）</th>
      <th>Scenario-specific Deployment Experience / 场景级部署经验</th>
      <th>Representative Scenarios / 代表场景</th>
      <th>Deployment Rationale / 部署经验解释</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td rowspan="3"><b>A</b></td>
      <td rowspan="3"><b>深流水线型</b>；<br><b>定性：</b><br>PP 较深；<br>TP 较小；<br>MB 较大；<br><b>主要取值/规则：</b><br>PP 取中等/较深；<br>TP 相对 PP 保持较小；<br>MB 随 PP 增大以填充流水线；</td>
      <td rowspan="3"><b>整体呈现局部化；双亲和组时常伴随明显组内失衡。</b><br>（14/14 个慢卡场景只覆盖 1–2/4 个亲和组；双亲和组场景中 6/7 的亲和组内两节点慢卡比例差 D<sub>intra</sub> ≥ 0.50。）</td>
      <td><b>A1 无慢卡基线</b></td>
      <td>1</td>
      <td><b>主要：</b><br>全局无慢卡：ρ = 0；<br><b>辅助：</b><br>无受影响节点：C<sub>N</sub> = 0；<br>无受影响亲和组：C<sub>A</sub> = 0；</td>
      <td>PP 不小于 4：PP ≥ 4；<br>TP 不大于 2：TP ≤ 2；<br>MB 不小于 2PP：MB ≥ 2PP；</td>
      <td>s001</td>
      <td>没有慢卡时，不需要专门处理快慢卡失衡，主要是在模型切分、同步开销和流水线利用率之间做基础权衡；<br>PP ≥ 4 把模型分成更多阶段，可降低单个阶段承担的模型和计算压力；<br>TP ≤ 2 避免同一层跨太多 GPU 做高频同步；<br>由于 PP 较深，MB ≥ 2PP 可以让更多微批次同时占据不同流水阶段，减少流水线空转；</td>
    </tr>
    <tr>
      <td><b>A2 单亲和组局部化</b></td>
      <td>7</td>
      <td><b>主要：</b><br>仅覆盖 1/4 个亲和组，慢卡明显局部化：C<sub>A</sub> = 0.25；<br><b>辅助：</b><br>影响 1–2/8 个节点：C<sub>N</sub> = 0.125–0.25；<br>当前样本全局慢卡率：ρ = 0.78%–21.88%；<br>单节点最高慢卡比例：R<sub>max</sub> = 6.25%–100%；<br>组内不均衡范围较宽：D<sub>intra</sub> = 0–0.875；</td>
      <td>PP 常不小于 4，或要求 TP 不大于 PP/2；<br>MB 常不小于 8，或不小于 2PP；<br>EP 多在 E/C 到约 2E/C；</td>
      <td>s003；<br>s012；<br>s013；<br>s023；</td>
      <td>慢卡只集中在一个亲和组，性能问题具有明显局部性，因此“把局部慢区和其他区域尽量解耦”是主要目标；<br>较小 TP 可以减少同一层中需要同步等待的 GPU 数量，避免一张慢卡让一个很大的 TP 组都停下来等待；<br>较深 PP 把模型切得更细，使局部慢卡的影响更容易限制在少数流水阶段，而不是把快慢 GPU 强绑定在同一个大同步组中；<br>深 PP 会增加流水线气泡，因此需要较大的 MB 把流水线填满，抵消新增阶段带来的空转；</td>
    </tr>
    <tr>
      <td><b>A3 双亲和组局部化 / 不均衡主导</b></td>
      <td>7</td>
      <td><b>主要：</b><br>覆盖 2/4 个亲和组：C<sub>A</sub> = 0.50；<br>组内不均衡明显，6/7 场景满足 D<sub>intra</sub> ≥ 0.50；<br><b>辅助：</b><br>影响 2–4/8 个节点：C<sub>N</sub> = 0.25–0.50；<br>当前样本全局慢卡率：ρ = 9.38%–41.41%；<br>单节点最高慢卡比例：R<sub>max</sub> = 25%–100%；</td>
      <td>TP 常不大于 PP/2；<br>PP 常不小于 4，或满足 PP ≥ 2TP；<br>MB 常不小于 2PP，或不小于 8；<br>保留各场景 EP 与上下界；</td>
      <td>s010；<br>s015；<br>s032；<br>s042；</td>
      <td>慢卡已经扩展到两个亲和组，但 6/7 场景仍存在很强的组内不均衡，因此主要瓶颈仍是少数局部慢节点造成的等待，而不是整个集群均匀变慢；<br>TP 保持较小，可以缩小每次张量同步的参与范围，减少慢节点对大量快节点的同步拖尾；<br>PP 保持中等或较深，使模型切分更细，在映射时有更多机会把慢节点的影响限制在少数阶段；<br>由于 PP 较深，MB 继续保持较大，用来提高流水线占用率并降低阶段气泡；</td>
    </tr>
    <tr>
      <td rowspan="3"><b>B</b></td>
      <td rowspan="3"><b>浅流水线型</b>；<br><b>定性：</b><br>PP 较浅；<br>TP 中等；<br>MB 较小；<br><b>主要取值/规则：</b><br>PP 常为 1 / 最小可行值；<br>TP 常约为 4，或由 TP/DP 比例约束；<br>MB 常为 1 / 最小可行值；</td>
      <td rowspan="3"><b>整体以广覆盖为主；部分覆盖时通常组内较均衡，或整体慢卡负担较高。</b><br>（71/84 个场景覆盖 4/4 个亲和组；其余 13 个中，8 个的亲和组内两节点慢卡比例差 D<sub>intra</sub> ≤ 0.25；另外 5 个的全局慢卡比例 ρ = 34.38%–61.72%，且单节点最高慢卡比例 R<sub>max</sub> = 1。）</td>
      <td><b>B1 全亲和组广泛覆盖</b></td>
      <td>71</td>
      <td><b>主要：</b><br>覆盖全部 4/4 个亲和组：C<sub>A</sub> = 1；<br><b>辅助：</b><br>至少一半节点受影响：C<sub>N</sub> = 0.50–1.00；<br>当前样本全局慢卡率：ρ = 3.13%–100%；<br>组内不均衡：D<sub>intra</sub> = 0–1（不作为该模式边界）；</td>
      <td>PP 通常取 1 或最小可行值；<br>TP 常约为 4，或受 TP/DP 比例约束；<br>MB 通常取 1 或最小可行值；<br>EP 多围绕 E/C；</td>
      <td>s021；<br>s038；<br>s081；<br>s100；</td>
      <td>慢卡已经覆盖全部亲和组，说明没有一个明显“快区”可以通过更深的 PP 单独保留下来，也没有少量局部慢区可以被干净隔离，因此继续增加 PP 的局部隔离收益明显下降；<br>PP 越深，模型需要经过的流水阶段越多，阶段之间更容易出现等待和气泡，所以此时更适合把 PP 降到 1 或很小；<br>PP 变浅后，模型仍需要在多张 GPU 上分摊单层计算，因此 TP 会承担更多模型内并行工作；TP 取中等值可以减少单卡计算压力，又避免 TP 过大导致过多高频同步；<br>剩余 GPU 更多地分配给 DP，用多份副本并行处理不同数据，提高整体吞吐，而不再增加流水线深度；<br>当 PP≈1 时几乎没有流水线需要填充，因此 MB 取 1 或最小值即可，继续增大 MB 的收益很小；</td>
    </tr>
    <tr>
      <td><b>B2 部分亲和组覆盖但组内较均衡</b></td>
      <td>8</td>
      <td><b>主要：</b><br>覆盖 2–3/4 个亲和组：C<sub>A</sub> = 0.50–0.75；<br>受影响亲和组内部较均衡：D<sub>intra</sub> ≤ 0.25；<br><b>辅助：</b><br>影响 4–6/8 个节点：C<sub>N</sub> = 0.50–0.75；<br>当前样本全局慢卡率：ρ = 6.25%–72.66%（不作为主要边界）；</td>
      <td>PP=1 倾向明显；<br>TP 常约为 4，或保持较低 TP/DP 比；<br>MB 通常为 1；<br>DP 由满卡关系补足；</td>
      <td>s007；<br>s033；<br>s043；<br>s063；</td>
      <td>这些场景虽然没有覆盖全部亲和组，但受影响亲和组内部两节点的慢卡比例接近，局部“一个很慢、一个很快”的失衡不明显；<br>因此没有很强的理由用深 PP 把某个单独慢节点隔离出来，增加 PP 反而会引入更多流水阶段和气泡；<br>较浅 PP 可以减少阶段间等待，TP 取中等值则负责分摊单层计算和显存压力；<br>TP 不取过大，是因为 TP 内部需要频繁同步，过大的 TP 会增加通信和同步等待；<br>PP 很浅时不需要大量微批次填流水线，因此 MB 取 1 更直接；</td>
    </tr>
    <tr>
      <td><b>B3 部分亲和组覆盖 + 中高密度 / 强局部集中</b></td>
      <td>5</td>
      <td><b>主要：</b><br>覆盖 2–3/4 个亲和组：C<sub>A</sub> = 0.50–0.75；<br>组内不均衡较高：D<sub>intra</sub> = 0.375–1.00；<br>慢卡总体负担较重：ρ = 34.38%–61.72%，且 R<sub>max</sub> = 1；<br><b>辅助：</b><br>影响 3–5/8 个节点：C<sub>N</sub> = 0.375–0.625；</td>
      <td>PP=1、MB=1 为主要方向；<br>TP 常约为 4，或由范围/比例约束；<br>DP 由 N/(PP × TP) 补足；</td>
      <td>s035；<br>s036；<br>s046；<br>s062；</td>
      <td>这类场景仍有明显局部不均衡，但慢卡已经占到约 34%–62%，而且至少存在一个全慢节点，说明性能问题不再只是少数异常卡，而是较大范围的整体降速；<br>当慢卡数量很多时，很难通过增加 PP 把所有慢卡都限制在少数阶段，局部隔离的收益因此下降；反而更深 PP 会增加阶段数、流水线气泡和阶段之间的等待；<br>所以 PP 倾向降到 1，先减少流水线本身的串行等待；<br>TP 取中等值，用来把单层计算分摊到多张 GPU，但不过度扩大同步组；<br>剩余 GPU 通过较大的 DP 扩展数据副本，提高并行吞吐；PP 很浅时 MB=1 也足够；</td>
    </tr>
  </tbody>
</table>

---

## 12. 主表各列分别回答什么问题？

| 列 | 主要职责 / 回答的问题 |
|---|---|
| Experience Family / 经验族 | 哪些部署经验可以归入同一个上层类别？ |
| Family Characteristics / 经验族部署特征（含主要部署经验） | 这一经验族属于哪一种并行结构类型，以及真正部署时最先抓住哪 2–3 条核心规则？先写“深流水线型/浅流水线型”等定性画像，再写主要 PP、TP、MB 取值或方向。 |
| Family-level Scenario Profile / 经验族场景总体特征 | 采用这种部署经验的场景，在慢卡分布上最主要的共同特点是什么？先给抽象结论，括号中只给支撑这些主特征的关键数据，不罗列辅助统计。 |
| Scenario Motif / 场景模式 | 经验族内部还能分成哪些典型慢卡分布形态？ |
| Scenario Count / 场景数量 | 每个场景模式包含多少个有效场景？ |
| Slow-GPU Distribution Features / 慢卡分布特征（主要/辅助） | 这个场景模式真正由哪些主特征区分？哪些指标只用于补充描述严重程度或边界？要求主要特征在前、辅助特征在后，并用中文物理含义说明变量。 |
| Scenario-specific Deployment Experience / 场景级部署经验 | 该场景模式在共同骨架之上还需要哪些具体范围、比例或约束？ |
| Representative Scenarios / 代表场景 | 哪些原始场景最适合解释和复核这一场景模式？ |
| Deployment Rationale / 部署经验解释 | 为什么该场景模式对应这样的部署经验？哪些是数据事实，哪些是解释性归纳？ |

其中三列最容易混淆，建议按下面的层级理解：

- **经验族部署特征（含主要部署经验）**回答“这是一种什么类型的并行结构，以及部署时首先抓住哪些主要规则”。先给“深流水线型 / 浅流水线型”的定性画像，再给 2–3 条主要 PP、TP、MB 取值或方向；
- **经验族场景总体特征**回答“这一类经验总体对应什么样的慢卡分布”，只概括主场景特征，并给支撑主特征的关键数据；
- **场景模式**回答“这些总体分布还能进一步分成哪些典型形态”。

---

## 13. 全部场景到场景模式的映射

> s005 为异常场景，不进入以下有效场景模式。

### A1（1 个）

s001

### A2（7 个）

s002, s003, s006, s012, s013, s022, s023

### A3（7 个）

s010, s015, s016, s025, s026, s032, s042

### B1（71 个）

s004, s008, s009, s011, s014, s017, s018, s019, s020, s021, s024, s027, s028, s029, s030, s031, s034, s037, s038, s039, s040, s041, s044, s045, s047, s048, s049, s050, s051, s054, s055, s057, s058, s059, s060, s061, s064, s065, s066, s067, s068, s069, s070, s071, s074, s075, s076, s077, s078, s079, s080, s081, s082, s083, s084, s085, s086, s087, s088, s089, s090, s091, s092, s093, s094, s095, s096, s097, s098, s099, s100

### B2（8 个）

s007, s033, s043, s052, s053, s063, s072, s073

### B3（5 个）

s035, s036, s046, s056, s062

---

## 14. 面向 256 GPU 的复用方式

应用到 256 GPU 时，不直接复用本报告中的“A/B 两个经验族”、当前阈值或 A1–B3 名称，而是复用同一流程：

1. 重新标准化 256 GPU 的拓扑参数；
2. 重新做异常筛选；
3. 重新选择最突出的部署经验主指标；
4. 重新形成经验族；
5. 用新的 $N,M,K,A,R$ 重新计算归一化慢卡特征；
6. 重新选择最突出的场景特征；
7. 重新形成场景模式；
8. 最后再总结经验族场景总体特征，并生成同样结构的 10 列主表。

这样，128 GPU 报告中的具体数值只作为当前实验实例，不会被错误固化成 256 GPU 的先验规则。
