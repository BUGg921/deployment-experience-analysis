# 256 GPU / 19 场景：部署经验与慢卡场景关系分析报告

> **分析依据**：`并行训练部署经验与慢卡场景归类_Skill.md`、`128GPU_100场景_部署经验与场景关系分析报告.md`、`256卡19场景最新报告合集_含部署经验.md`。  
> **分析原则**：严格保持“部署经验公式 → 经验族 → 场景模式解释”的方向。打分最优与仿真最优只用于异常检查与后验验证，不用于反向构造经验族。

## 1. 核心术语中英文对照

| English | 中文 |
|---|---|
| Experience Family | 经验族 / 部署经验族 |
| Family Characteristics | 经验族部署特征（含主要部署经验） |
| Family-level Scenario Profile | 经验族场景总体特征 |
| Scenario Motif | 场景模式 |
| Scenario Count | 场景数量 |
| Slow-GPU Distribution Features | 慢卡分布特征 |
| Scenario-specific Deployment Experience | 场景级部署经验 |
| Deployment Rationale | 部署经验解释 |
| Representative Scenarios | 代表场景 |

---

## 2. Step 1 — Input Normalization / 输入标准化

本批 256 GPU 实验统一拓扑为：

- 总 GPU 数：N = 256；
- 节点数：M = 16；
- 每节点 GPU 数：K = 16；
- 亲和组数量：A = 8；
- 每个亲和组包含 R = 2 个节点，即 32 GPU；
- 全局 batch：B = 128；
- 专家数 E = 192；
- 单 EP rank 专家容量上限 C = 24。

统一关系为：

$$
PP\times TP\times DP=256,
$$

因此：

$$
DP=\frac{256}{PP\times TP}.
$$

专家容量约束给出：

$$
p_E=\frac{E}{EP\times C}=\frac{8}{EP}\le1
\quad\Rightarrow\quad
EP\ge8.
$$

batch 可行性约束至少给出：

$$
DP\times MB\le128.
$$

代入 DP 后：

$$
\frac{MB}{PP}\le\frac{TP}{2}.
$$

这条关系来自**可行性约束**，不属于 score 偏好。

本批候选均使用满卡，因此 `active_gpu = 256` 是共同约束，不作为第一层经验族区分指标。

---

## 3. Step 2 — Anomaly Screening / 异常筛选与隔离

### 3.1 数据一致性检查

19 个场景均满足：

$$
S=\sum_i c_i=\sum_j s_j,
$$

且节点慢卡数均位于 `[0,16]`，亲和组慢卡数均位于 `[0,32]`。

### 3.2 打分与仿真一致性检查

19 个场景的评分摘要 Top1 与仿真最优全部一致；所有报告状态均为 `PASS_DEBUG`。

### 3.3 经验公式异常检查

未发现类似 128 GPU 报告中“经验公式明确排除本场景自身最优策略”的已确认异常。19 个场景的部署经验总结与报告中给出的当前离散参数收敛结果相容，因此本报告保留全部 19 个场景进入归纳。

> 注意：这只说明当前报告内部未发现已确认矛盾，不代表评分函数已经获得独立泛化验证。

**有效场景数：19。**

---

## 4. Step 3 — Experience Feature Selection / 部署经验特征筛选

本批数据重新执行特征筛选，没有直接沿用 128 GPU 的结论。

### 4.1 主指标

对 PP / TP / DP / EP / MB / active GPU 逐一比较后，最小解释集合仍主要落在：

$$
\{PP,TP,MB\}.
$$

但与 128 GPU 不同的是，256 GPU 中“深流水线”内部进一步出现了两种稳定方向：

1. **TP 与 PP 同量级、MB 较大**；
2. **TP 被进一步压低、MB 约为 PP 量级**。

因此 TP 相对于 PP 的比例在本批数据中具有更强区分力。

### 4.2 辅助指标

**DP** 主要由：

$$
DP=\frac{256}{PP\times TP}
$$

派生，因此更适合作为辅助规则；当某些 score 明确奖励 `DP ≥ 40 / 64` 时，可进一步消元为：

$$
PP\times TP\le \frac{256}{40}=6.4,
$$

或：

$$
PP\times TP\le4.
$$

**EP** 大多数经验集中在 `E/C = 8` 附近，常见范围为 `[E/C, 4E/(3C)]`、`[E/C, 10E/(9C)]` 或 `[E/C, 2E/C)`，区分度弱于 PP/TP/MB。

**active GPU** 在全部场景中均为 256，是共同约束。

因此，本批 256 GPU 的经验族主/辅指标为：

$$
\{PP,TP,MB\}\quad\text{主指标},
$$

$$
\{DP,EP,active\}\quad\text{辅助规则}.
$$

---

## 5. Step 4 — Experience Family Construction / 部署经验族划分

仅根据部署经验公式，可将 19 个场景压缩为 3 个上层经验族。

| 经验族 | 场景数 | 经验族部署特征 | 成员场景 |
|---|---:|---|---|
| A | 6 | **深流水线–中等 TP–大 MB 型**；PP 倾向 4/较深；TP 通常不超过 PP、常落在 4 附近；MB 通常随 PP 增大，常要求 `MB ≥ 2PP` 或 `MB ≥ max(PP,8)`。 | s005、s006、s010、s026、s043、s046 |
| B | 2 | **深流水线–低 TP–中等 MB 型**；相较 A 进一步压低 TP；TP 常要求 `≤2` 或 `≤0.75PP`；MB 更接近 PP 量级，而不是 2PP。 | s025、s042 |
| C | 11 | **浅流水线–高 DP–小 MB 型**；PP 常为 1 或由高 DP 约束被压到很浅；TP 保持中等规模、常在 4 附近；MB 常为 1。 | s004、s009、s031、s037、s045、s049、s052、s073、s090、s092、s093 |

这里的 A/B/C 是**从部署经验公式形成的**，不是按慢卡覆盖范围预先命名。

---

## 6. Step 5 — Family-level Experience Abstraction / 经验族级部署经验抽象

### 6.1 经验族 A：深流水线–中等 TP–大 MB 型

主要经验：

- PP：倾向较深，当前搜索空间中大量公式指向 `PP ≥ 4`；
- TP：与 PP 同量级但不超过 PP，常见 `TP ≤ PP`、`TP ≤ 4`；
- MB：明显偏大，常见 `MB ≥ 2PP`、`MB ≥ max(PP,8)`，或要求 `MB/PP` 尽量大。

辅助规则：

- 当前离散空间通常收敛为 `PP4_TP4_DP16_MB8`；
- EP 多在容量下界 `E/C` 附近。

### 6.2 经验族 B：深流水线–低 TP–中等 MB 型

主要经验：

- PP：仍保持深流水线方向；
- TP：比 A 明显更小，s025 优先 `TP ≤ 2`，s042 要求 `TP ≤ 0.75PP`；
- MB：保持中等，通常约为 PP 量级，而不是 A 中常见的 `2PP`。

辅助规则：

- 当前约束共同落在 `PP4_TP2_DP32_MB4`；
- EP 仍接近 `E/C`。

### 6.3 经验族 C：浅流水线–高 DP–小 MB 型

主要经验：

- PP：倾向 1/最浅可行值；
- TP：保持中等规模，常见 `TP ≤ 4/6/8/10`，并在高 DP 区域内尽量增大 `TP/DP`；
- MB：明显偏小，常为 `MB = 1`，或由 `0.65 ≤ MB/PP ≤ 1.35` 与浅 PP 联合收敛到 1。

辅助规则：

- 多个场景直接要求 `DP ≥ 36/40/64`，等价于限制 `PP×TP` 不宜过大；
- 当前离散空间大量收敛到 `PP1_TP4_DP64_MB1`；
- EP 多在 `E/C` 附近。

---

## 7. Step 6 — Scenario Feature Construction / 场景特征构造

沿用 Skill 的无量纲定义：

### 7.1 全局慢卡率

$$
\rho=\frac{S}{256}.
$$

### 7.2 节点覆盖率

$$
C_N=\frac{\#\{i:c_i>0\}}{16}.
$$

### 7.3 亲和组覆盖率

$$
C_A=\frac{\#\{j:s_j>0\}}{8}.
$$

### 7.4 单节点最大慢卡比例

$$
R_{\max}=\max_i\frac{c_i}{16}.
$$

### 7.5 亲和组内不均衡

每个亲和组包含两个节点，因此：

$$
D_{\mathrm{intra},j}
=
\left|\frac{c_{j,1}}{16}-\frac{c_{j,2}}{16}\right|,
$$

$$
D_{\mathrm{intra}}
=
\max_jD_{\mathrm{intra},j}.
$$

### 7.6 亲和组间不均衡

定义亲和组慢卡比例：

$$
a_j=\frac{s_j}{32},
$$

则：

$$
D_{\mathrm{aff}}
=
\frac{2}{8(8-1)}
\sum_{p<q}|a_p-a_q|.
$$

---

## 8. Step 7 — Scenario Feature Selection / 场景特征筛选

### 8.1 当前 256 GPU 数据中的主要结构特征

整体上最有解释力的仍是：

1. **亲和组覆盖率 `C_A`**：区分局部化与广覆盖；
2. **节点覆盖率 `C_N`**：在 8 个亲和组规模下，补充说明慢卡究竟只落在少数节点还是扩散到大部分节点；
3. **亲和组内不均衡 `D_intra`**：对部分边界场景非常关键。

`ρ`、`R_max`、`D_aff` 更多用于描述负担强度和边界，但在近饱和场景中 `ρ` 会升级为主要区别特征。

### 8.2 有信息量的边界对照

#### s042 与 s043：覆盖范围几乎相同，但组内结构不同

- s042（Family B）：`ρ = 41.41%`，`C_N = 0.50`，`C_A = 0.50`，`R_max = 1`，`D_intra = 0.6875`；
- s043（Family A）：`ρ = 42.19%`，`C_N = 0.50`，`C_A = 0.50`，`R_max = 1`，`D_intra = 0`。

这组对照说明：当总慢卡率和覆盖范围接近时，亲和组内部是否失衡可能对应不同的 TP 压缩强度。

#### s025 与 s026：基础六特征几乎无法区分，但经验族不同

- s025（Family B）：`ρ = 24.22%`，`C_N = 0.25`，`C_A = 0.50`，`R_max = 1`，`D_intra = 1`，`D_aff = 0.28125`；
- s026（Family A）：`ρ = 25.00%`，`C_N = 0.25`，`C_A = 0.50`，`R_max = 1`，`D_intra = 1`，`D_aff ≈ 0.28571`。

两者在当前六个基础特征上几乎重合，却分别对应低 TP Family B 与中等 TP Family A。

**结论**：这六个特征足以做场景模式解释，但不能被误用为确定性的 Family 分类器。若后续目标变成“仅从慢卡布局预测部署经验”，还需要增加能够描述亲和组位置/邻接关系、并行组暴露范围等拓扑结构特征；本报告不在缺少定义的情况下自行构造这些特征。

---

## 9. Step 8 — Scenario Motif Extraction / 场景模式提取

### 9.1 Family A

#### A1 — 双亲和组稀疏局部化

场景：s005、s006，共 2 个。

主要区别特征：

- 仅覆盖 2/8 个亲和组：`C_A = 0.25`；
- 仅覆盖 2/16 个节点：`C_N = 0.125`。

辅助特征：

- `ρ = 3.91%–4.69%`；
- `R_max = 31.25%–37.50%`；
- `D_intra = 0.3125–0.375`。

场景级经验：

- 继承 Family A 的深 PP、大 MB 骨架；
- TP 不超过 PP/4 附近中等规模；
- MB 倾向 `max(PP,8)` 或提高 `MB/PP`；
- 当前离散约束均收敛到 PP4、TP4、MB8。

#### A2 — 四亲和组局部化

场景：s010、s026、s043、s046，共 4 个。

主要区别特征：

- 覆盖 4/8 个亲和组：`C_A = 0.50`；
- 节点覆盖仍不超过一半：`C_N = 0.25–0.50`。

辅助特征：

- `ρ = 9.38%–45.31%`；
- `R_max = 25%–100%`；
- `D_intra = 0–1`，跨度很大，因此不是本 Motif 的硬边界。

场景级经验：

- PP 保持深：常见 `PP ≥ 4`；
- TP 常要求 `TP ≤ PP` 或 `TP ≤ 4`；
- MB 常要求 `MB ≥ 2PP` 或 `MB ≥ max(PP,8)`；
- EP 主要在 `E/C` 附近。

### 9.2 Family B

#### B1 — 半亲和组覆盖 + 强组内不均衡

场景：s025、s042，共 2 个。

主要区别特征：

- 覆盖 4/8 个亲和组：`C_A = 0.50`；
- 单节点最大慢卡比例达到 100%：`R_max = 1`；
- 亲和组内不均衡明显：`D_intra = 0.6875–1`。

辅助特征：

- `C_N = 0.25–0.50`；
- `ρ = 24.22%–41.41%`；
- `D_aff = 0.28125–0.52232`。

场景级经验：

- 相较 Family A 进一步压缩 TP：s025 优先 `TP ≤ 2`，s042 要求 `TP ≤ 0.75PP`；
- MB 约为 PP 量级：s025 的 `MB ≥ PP` 仅为弱 tie-break，s042 要求 `MB/PP ≥ 0.5` 且尽量增大；
- 当前离散约束共同落在 PP4、TP2、DP32、MB4。

### 9.3 Family C

#### C1 — 全亲和组广覆盖 / 低至中等负担

场景：s004、s009、s031、s037、s049，共 5 个。

主要区别特征：

- 覆盖全部 8/8 个亲和组：`C_A = 1`；
- 亲和组内不均衡总体不高：`D_intra = 0.0625–0.375`。

辅助特征：

- `C_N = 0.50–1.00`；
- `ρ = 3.13%–48.44%`，跨度较大，因此不作为本 Motif 的主要边界；
- `R_max = 6.25%–75%`。

场景级经验：

- PP 倾向 1/最浅；
- 保持高 DP，并在高 DP 区域内适度提高 TP/DP；
- TP 多落在 4 左右；
- MB = 1 或随 PP=1 收敛到 1。

#### C2 — 全亲和组覆盖 + 组内强单节点集中（边界/特殊）

场景：s045，共 1 个。

主要区别特征：

- 覆盖全部 8/8 个亲和组：`C_A = 1`；
- 但仅一半节点受影响：`C_N = 0.50`；
- `D_intra = 1`，表示受影响亲和组内出现“一个节点全慢、另一个节点正常”的极端差异。

辅助特征：

- `ρ = 44.53%`；
- `R_max = 1`。

场景级经验：

- `PP = 1`；
- `TP ≤ 4`；
- `DP×MB ≤ 2B/3`；
- EP 位于 `[E/C, 2E/C)`；
- 在当前可行域中仍表现为浅 PP、小 MB、高 DP 方向。

由于仅有单一样本，本 Motif 不应被泛化为稳定硬阈值。

#### C3 — 六亲和组覆盖 + 高负担局部缺口

场景：s052、s073，共 2 个。

主要区别特征：

- 覆盖 6/8 个亲和组：`C_A = 0.75`；
- 单节点最大慢卡比例达到 100%：`R_max = 1`；
- 未覆盖亲和组形成明显局部缺口。

辅助特征：

- `C_N = 0.625–0.75`；
- `ρ = 51.56%–72.66%`；
- `D_intra = 0.0625–0.125`，说明受影响组内部反而较均衡；
- `D_aff = 0.44196–0.56250`，亲和组之间差异较大。

场景级经验：

- 继续采用浅 PP、高 DP；
- s052 明确要求 `DP ≥ 64`、`0.65 ≤ MB/PP ≤ 1.35`；
- s073 明确要求 `MB = 1`、`2.6 ≤ TP/PP ≤ 5.4` 且 `PP×TP² ≤ N/10`；
- 两者都支持小 MB、适中 TP、浅 PP 的骨架。

#### C4 — 全亲和组近饱和

场景：s090、s092、s093，共 3 个。

主要区别特征：

- 覆盖全部 8/8 个亲和组：`C_A = 1`；
- 所有 16 个节点均受影响：`C_N = 1`；
- 全局慢卡率极高：`ρ = 89.84%–92.97%`；
- 单节点最大慢卡比例为 100%：`R_max = 1`。

辅助特征：

- `D_intra = 0.0625–0.625`，说明在近饱和状态下组内差异不再是稳定边界。

场景级经验：

- PP 明确保持极浅，s092/s093 直接给出 `PP = 1`；
- TP 保持中等规模，约束为 `TP ≤ 6/8` 或直接 `TP = 4`；
- DP 保持较高，常见 `DP > 16`、`DP ≥ 40` 或直接 `DP = 64`；
- MB 仍偏向 1。

---

## 10. Step 9 — Family-level Scenario Abstraction / 经验族场景总体特征抽象

### Family A

**局部化、只覆盖少数亲和组**。全部 6 个场景仅覆盖 2/8 或 4/8 个亲和组，节点覆盖不超过一半。组内不均衡可以从 0 到 1 大幅变化，因此“局部化覆盖”比 `D_intra` 更稳定。

### Family B

**半亲和组覆盖、局部单节点负担极重且组内不均衡明显**。两个场景均覆盖 4/8 个亲和组、`R_max = 1`，且 `D_intra ≥ 0.6875`。但该 Family 只有 2 个样本，应谨慎泛化。

### Family C

**广覆盖为主**。11 个场景中 9 个覆盖全部 8 个亲和组，另外 2 个覆盖 6/8；所有场景节点覆盖率至少 50%。随着慢卡率升高，Family C 从“全组稀疏扩散”逐渐过渡到“局部缺口高负担”和“全节点近饱和”。

---

## 11. 并行参数对性能的直观影响

- **PP**：增大 PP 会把模型拆成更多流水阶段，可能把局部慢区影响限制在少数阶段，并降低单阶段模型/显存压力；代价是更多流水气泡、跨阶段传输与等待。
- **TP**：增大 TP 会让更多 GPU 共同计算同一层，但同步更频繁；若某个 TP 组里有慢卡，整个组更容易等待。因此局部慢卡明显时，压低 TP 往往更安全。
- **DP**：增大 DP 会增加数据并行副本。当前数据中 DP 很多时候不是独立选择，而是由 `DP = 256/(PP×TP)` 派生。
- **MB**：深 PP 需要更多微批次填充流水线以降低气泡比例；PP 很浅时，MB 可以更小。
- **EP**：本批数据中主要受专家容量约束影响，绝大多数经验都集中在 `E/C = 8` 附近，因此不是主要 Family 区分维度。

下面“部署经验解释”用于帮助理解性能机制，不等同于评分公式的严格数学证明。

---

## 12. Family–Scenario–Experience 对照表

<table>
<thead>
<tr>
<th>Experience Family / 经验族</th>
<th>Family Characteristics / 经验族部署特征</th>
<th>Family-level Scenario Profile / 经验族场景总体特征</th>
<th>Scenario Motif / 场景模式</th>
<th>Scenario Count / 场景数量</th>
<th>Slow-GPU Distribution Features / 慢卡分布特征</th>
<th>Scenario-specific Deployment Experience / 场景级部署经验</th>
<th>Representative Scenarios / 代表场景</th>
<th>Deployment Rationale / 部署经验解释</th>
</tr>
</thead>
<tbody>
<tr>
<td rowspan="2"><b>A</b></td>
<td rowspan="2"><b>深流水线–中等 TP–大 MB 型</b>；<br>PP 倾向 4/较深；<br>TP 通常不超过 PP、常在 4 附近；<br>MB 常随 PP 增大，典型为 MB ≥ 2PP 或 MB ≥ max(PP,8)。</td>
<td rowspan="2"><b>局部化、少数亲和组覆盖</b>；<br>全部场景只覆盖 2/8 或 4/8 个亲和组；<br>节点覆盖不超过 1/2。</td>
<td><b>A1 — 双亲和组稀疏局部化</b></td>
<td>2</td>
<td><b>主要：</b><br>覆盖 2/8 个亲和组：C<sub>A</sub> = 0.25；<br>覆盖 2/16 个节点：C<sub>N</sub> = 0.125。<br><b>辅助：</b><br>全局慢卡率 ρ = 3.91%–4.69%；<br>单节点最大慢卡比例 R<sub>max</sub> = 31.25%–37.50%；<br>组内不均衡 D<sub>intra</sub> = 0.3125–0.375。</td>
<td>继承深 PP 骨架；<br>TP ≤ PP / 中等规模；<br>MB 偏大，取 max(PP,8) 或提高 MB/PP；<br>EP 约为 E/C。</td>
<td>s005、s006</td>
<td>慢卡高度局部化时，更深 PP 有机会把受影响资源限制在较少流水阶段；TP 不继续扩大，可减少慢卡进入大同步组后的连带等待。深流水线带来的气泡需要更大的 MB 补偿，因此 MB 明显高于浅流水线 Family。</td>
</tr>

<tr>
<td><b>A2 — 四亲和组局部化</b></td>
<td>4</td>
<td><b>主要：</b><br>覆盖 4/8 个亲和组：C<sub>A</sub> = 0.50；<br>节点覆盖 C<sub>N</sub> = 0.25–0.50。<br><b>辅助：</b><br>ρ = 9.38%–45.31%；<br>R<sub>max</sub> = 25%–100%；<br>D<sub>intra</sub> = 0–1。</td>
<td>PP ≥ 4 为主；<br>TP ≤ PP 或 TP ≤ 4；<br>MB ≥ 2PP 或 MB ≥ max(PP,8)；<br>EP 主要靠近 E/C。</td>
<td>s010、s026、s043、s046</td>
<td>慢卡仍只落在一半亲和组以内，保留较深 PP 仍有局部隔离价值；但组内不均衡本身并不稳定，因此 Family A 的关键不是某个固定 D<sub>intra</sub> 阈值，而是“局部化覆盖 + 深 PP + 大 MB”的组合。</td>
</tr>

<tr>
<td><b>B</b></td>
<td><b>深流水线–低 TP–中等 MB 型</b>；<br>PP 仍偏深；<br>TP 比 Family A 进一步压低；<br>MB 更接近 PP 量级。</td>
<td><b>半亲和组覆盖、强局部集中</b>；<br>覆盖 4/8 个亲和组；<br>单节点存在全慢；<br>组内不均衡明显。</td>
<td><b>B1 — 半亲和组覆盖 + 强组内不均衡</b></td>
<td>2</td>
<td><b>主要：</b><br>覆盖 4/8 个亲和组：C<sub>A</sub> = 0.50；<br>单节点最大慢卡比例 R<sub>max</sub> = 1；<br>D<sub>intra</sub> = 0.6875–1。<br><b>辅助：</b><br>C<sub>N</sub> = 0.25–0.50；<br>ρ = 24.22%–41.41%。</td>
<td>s025：优先 TP ≤ 2；<br>s042：TP ≤ 0.75PP；<br>MB 约为 PP 量级；<br>当前离散约束落在 PP4、TP2、DP32、MB4。</td>
<td>s025、s042</td>
<td>这类场景至少存在一个节点负担极重，若 TP 组过大，一张慢卡会拖慢更多同步伙伴，因此比 Family A 更积极压低 TP。PP 仍较深，用于保留局部划分能力；MB 维持中等即可，在填充流水线与显存/批约束之间折中。</td>
</tr>

<tr>
<td rowspan="4"><b>C</b></td>
<td rowspan="4"><b>浅流水线–高 DP–小 MB 型</b>；<br>PP 常为 1/最浅；<br>TP 适中、常在 4 附近；<br>MB 常为 1；<br>DP 由较小 PP×TP 派生为较大值。</td>
<td rowspan="4"><b>广覆盖为主</b>；<br>9/11 场景覆盖全部 8 个亲和组；<br>其余 2 个覆盖 6/8；<br>节点覆盖率均 ≥ 50%。</td>
<td><b>C1 — 全亲和组广覆盖 / 低至中等负担</b></td>
<td>5</td>
<td><b>主要：</b><br>覆盖全部 8/8 个亲和组：C<sub>A</sub> = 1；<br>D<sub>intra</sub> = 0.0625–0.375。<br><b>辅助：</b><br>C<sub>N</sub> = 0.50–1；<br>ρ = 3.13%–48.44%；<br>R<sub>max</sub> = 6.25%–75%。</td>
<td>PP 倾向 1；<br>保持高 DP；<br>高 DP 区域内适度提高 TP/DP；<br>TP 多在 4 附近；<br>MB = 1。</td>
<td>s004、s009、s031、s037、s049</td>
<td>慢卡已经扩散到全部亲和组，继续增加 PP 很难把慢卡只隔离在少数流水阶段，新增阶段反而会增加气泡和阶段等待，因此浅 PP 更合理。释放出的 GPU 主要转入 DP；TP 保持中等，避免同步组过大。</td>
</tr>

<tr>
<td><b>C2 — 全亲和组覆盖 + 组内强单节点集中（边界/特殊）</b></td>
<td>1</td>
<td><b>主要：</b><br>C<sub>A</sub> = 1；<br>C<sub>N</sub> = 0.50；<br>D<sub>intra</sub> = 1。<br><b>辅助：</b><br>ρ = 44.53%；<br>R<sub>max</sub> = 1。</td>
<td>PP = 1；<br>TP ≤ 4；<br>DP×MB ≤ 2B/3；<br>EP ∈ [E/C,2E/C)。</td>
<td>s045</td>
<td>虽然每个亲和组都受影响，但慢卡集中在组内单个节点。由于慢卡已经遍布所有组，深 PP 隔离收益仍有限；同时控制 TP 可减小单个全慢节点对同步组的放大效应。单样本仅作为边界模式保留。</td>
</tr>

<tr>
<td><b>C3 — 六亲和组覆盖 + 高负担局部缺口</b></td>
<td>2</td>
<td><b>主要：</b><br>覆盖 6/8 个亲和组：C<sub>A</sub> = 0.75；<br>R<sub>max</sub> = 1。<br><b>辅助：</b><br>C<sub>N</sub> = 0.625–0.75；<br>ρ = 51.56%–72.66%；<br>D<sub>intra</sub> = 0.0625–0.125；<br>D<sub>aff</sub> = 0.44196–0.56250。</td>
<td>浅 PP、高 DP；<br>s052：DP ≥ 64、0.65 ≤ MB/PP ≤ 1.35；<br>s073：MB = 1、2.6 ≤ TP/PP ≤ 5.4、PP×TP² ≤ N/10。</td>
<td>s052、s073</td>
<td>虽然仍有两个未受影响亲和组，但大多数亲和组已经承受较高慢卡负担，继续靠更深 PP 做局部隔离的空间有限。浅 PP 减少流水等待，高 DP 承接并行度；小 MB 与浅流水线相匹配。</td>
</tr>

<tr>
<td><b>C4 — 全亲和组近饱和</b></td>
<td>3</td>
<td><b>主要：</b><br>C<sub>A</sub> = 1；<br>C<sub>N</sub> = 1；<br>ρ = 89.84%–92.97%；<br>R<sub>max</sub> = 1。<br><b>辅助：</b><br>D<sub>intra</sub> = 0.0625–0.625。</td>
<td>PP = 1/极浅；<br>TP 维持中等，约 TP ≤ 6/8 或 TP = 4；<br>DP 保持较高；<br>MB 偏向 1。</td>
<td>s090、s092、s093</td>
<td>几乎所有节点都含慢卡，此时不存在可被流水线切分“躲开”的明显快区。增加 PP 只会增加阶段依赖与气泡，因此经验稳定转向最浅 PP。TP 不宜过大，DP 则由较小 PP×TP 自动扩大。</td>
</tr>
</tbody>
</table>

---

## 13. 关键结论

1. **256 GPU 不能直接复制 128 GPU 的“两经验族”结论。** 当前 19 个场景的经验公式支持三个 Family，其中深流水线又分为“中等 TP + 大 MB”和“更低 TP + 中等 MB”两类。
2. **PP/TP/MB 仍是最小解释集合，但 TP/PP 关系在 256 GPU 中更重要。**
3. **DP 主要是派生维度。** 高 DP 偏好应尽量改写为 PP×TP 的直接约束，而不是把 DP 当作独立经验。
4. **Family A/B 主要对应局部化场景，Family C 主要对应广覆盖场景，但这种关系不是确定性分类规则。**
5. **s025 与 s026 是关键反例。** 六个基础慢卡特征几乎相同，但经验族不同，说明要从慢卡布局直接预测部署经验，还需要更细的拓扑/并行组暴露特征。
6. **Family C 的内部变化主要体现“广覆盖程度和负担强度”的递进**：全组稀疏扩散 → 全组局部强集中（边界）/六组高负担 → 全节点近饱和。

---

## 附录 A：19 个场景的归一化慢卡特征

| 场景 | Family | ρ | C_N | C_A | R_max | D_intra | D_aff |
|---|---|---:|---:|---:|---:|---:|---:|
| s004 | C | 3.12% | 0.500 | 1.000 | 0.062 | 0.062 | 0.000 |
| s005 | A | 3.91% | 0.125 | 0.250 | 0.312 | 0.312 | 0.067 |
| s006 | A | 4.69% | 0.125 | 0.250 | 0.375 | 0.375 | 0.080 |
| s009 | C | 7.81% | 0.875 | 1.000 | 0.125 | 0.062 | 0.027 |
| s010 | A | 9.38% | 0.375 | 0.500 | 0.250 | 0.250 | 0.125 |
| s025 | B | 24.22% | 0.250 | 0.500 | 1.000 | 1.000 | 0.281 |
| s026 | A | 25.00% | 0.250 | 0.500 | 1.000 | 1.000 | 0.286 |
| s031 | C | 30.47% | 1.000 | 1.000 | 0.312 | 0.062 | 0.013 |
| s037 | C | 36.72% | 1.000 | 1.000 | 0.625 | 0.125 | 0.228 |
| s042 | B | 41.41% | 0.500 | 0.500 | 1.000 | 0.688 | 0.522 |
| s043 | A | 42.19% | 0.500 | 0.500 | 1.000 | 0.000 | 0.527 |
| s045 | C | 44.53% | 0.500 | 1.000 | 1.000 | 1.000 | 0.094 |
| s046 | A | 45.31% | 0.500 | 0.500 | 1.000 | 0.375 | 0.545 |
| s049 | C | 48.44% | 1.000 | 1.000 | 0.750 | 0.375 | 0.134 |
| s052 | C | 51.56% | 0.625 | 0.750 | 1.000 | 0.125 | 0.562 |
| s073 | C | 72.66% | 0.750 | 0.750 | 1.000 | 0.062 | 0.442 |
| s090 | C | 89.84% | 1.000 | 1.000 | 1.000 | 0.250 | 0.121 |
| s092 | C | 92.19% | 1.000 | 1.000 | 1.000 | 0.625 | 0.134 |
| s093 | C | 92.97% | 1.000 | 1.000 | 1.000 | 0.062 | 0.121 |
